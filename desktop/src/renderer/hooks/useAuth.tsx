/**
 * Re-exports Clerk's auth hooks for convenience.
 *
 * Components can import from here to keep a consistent local import path,
 * or import directly from @clerk/clerk-react.
 */

export { useAuth, useUser, useClerk } from '@clerk/clerk-react';
